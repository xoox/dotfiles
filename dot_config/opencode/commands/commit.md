---
description: git commit
subtask: true
---

- Generate a git commit message based on the provided git diff and status. Strictly follow these formatting and style guidelines.
- Then commit in the current staging changes use the generated commit message.

## Commit Message

Use `commit-message` skill to write/form commit message.

## Conflicts

- If there are git conflicts, DO NOT FIX THEM. Notify me immediately and I will fix them.

## GIT DIFF

!`git diff`

## GIT DIFF --cached

!`git diff --cached`

## GIT STATUS --short

!`git status --short`
